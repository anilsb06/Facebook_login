
trigger:
 - main

pool:
  name: default
  vmImage: ubuntu-g-2vcpu-8gb-blr1-01

steps:

   - task: CmdLine@2
     inputs:
       script: 'docker build . -t anilsb06/azurepipeline-login2'

   - task: CmdLine@2
     inputs:
       script: |
         docker login -u anilsb06 -p Anil@9591096425
         docker push anilsb06/azurepipeline-login2
   
   - task: SSH@0
     inputs:
       sshEndpoint: 'ssh-service -coonection'
       runOptions: 'commands'
       commands: 'kubectl apply -f fb.yml'
       readyTimeout: '20000'
  
  
