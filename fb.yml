apiVersion: apps/v1
kind: Deployment
metadata:
  name: login-page3
spec:
  replicas: 1
  selector:     
    matchLabels:
     app: myos
  template:
    metadata:
      labels:
        app: myos
    spec:
      containers:
      - name: login
        image: facebok_login1
    

