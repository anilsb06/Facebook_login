apiVersion: apps/v1
kind: Deployment
metadata:
  name: login-page4
spec:
  replicas: 1
  selector:     
    matchLabels:
     app: myos
  template:
    metadata:
      labels:
        app: myos
    spec:
      containers:
      - name: login
        image: anilsb06/login_page2
    

